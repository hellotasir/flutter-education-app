import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const white = Colors.white;
  static const black = Colors.black;

  static const lightBackground = Colors.white;
  static const darkBackground = Color(0xFF0A0A0A);

  static const lightSurface = Colors.white;
  static const darkSurface = Color(0xFF121212);

  static const lightBorder = Colors.black12;
  static const darkBorder = Colors.white24;

  static const lightText = Colors.black;
  static const darkText = Colors.white;

  static const lightPrimary = Color(0xFF1A1A1A);
  static const lightPrimaryVariant = Color(0xFF333333);
  static const lightSecondary = Color(0xFF444444);
  static const lightSecondaryVariant = Color(0xFF555555);

  static const darkPrimary = Color(0xFFE0E0E0);
  static const darkPrimaryVariant = Color(0xFFBDBDBD);
  static const darkSecondary = Color(0xFFB0B0B0);
  static const darkSecondaryVariant = Color(0xFF9E9E9E);

  static const lightOnPrimary = Colors.white;
  static const lightOnSecondary = Colors.white;
  static const lightOnSurface = Color(0xFF1A1A1A);
  static const lightOnBackground = Color(0xFF1A1A1A);
  static const lightOnError = Colors.white;

  static const darkOnPrimary = Color(0xFF0A0A0A);
  static const darkOnSecondary = Color(0xFF0A0A0A);
  static const darkOnSurface = Color(0xFFE0E0E0);
  static const darkOnBackground = Color(0xFFE0E0E0);
  static const darkOnError = Colors.white;

  static const lightError = Color(0xFFB00020);
  static const darkError = Color(0xFFCF6679);

  static const lightHint = Color(0xFF9E9E9E);
  static const darkHint = Color(0xFF616161);

  static const lightDisabled = Color(0xFFBDBDBD);
  static const darkDisabled = Color(0xFF424242);

  static const lightIcon = Color(0xFF424242);
  static const darkIcon = Color(0xFFBDBDBD);

  static const lightChipBackground = Color(0xFFF5F5F5);
  static const darkChipBackground = Color(0xFF2C2C2C);

  static const lightTableHeader = Color(0xFFFAFAFA);
  static const darkTableHeader = Color(0xFF1E1E1E);

  static const lightInfoBackground = Color(0xFFF5F5F5);
  static const darkInfoBackground = Color(0xFF1E1E1E);

  static const lightSnackBarBackground = Color(0xFF1A1A1A);
  static const lightSnackBarContent = Colors.white;
  static const darkSnackBarBackground = Color(0xFFE0E0E0);
  static const darkSnackBarContent = Color(0xFF0A0A0A);

  static const lightTooltipBackground = Color(0xFF1A1A1A);
  static const lightTooltipContent = Colors.white;
  static const darkTooltipBackground = Color(0xFFE0E0E0);
  static const darkTooltipContent = Color(0xFF0A0A0A);
}
